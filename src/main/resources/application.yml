spring:
  profiles:
    active: dev
---
spring:
  profiles: dev
  data:
    mongodb:
      host: localhost
      port: 27017
      database: local
    #uri: mongodb+srv://admin:admin@cluster0.rmoh4.azure.mongodb.net/test?retryWrites=true&w=majority

---
spring:
  profiles: qa
  data:
    mongodb:
      host: localhost
      port: 27017
      database: local
---
spring:
  profiles: prod
  data:
    mongodb:
      host: localhost
      port: 27017
      database: local
